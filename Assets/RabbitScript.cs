using UnityEngine;
using System.Collections;

public class RabbitScript : MonoBehaviour {

	int frames = 30;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		if (frames > 8 && frames < 27) {
//			transform.Translate(new Vector3(-0.02f,0,0));
//		}
//
//		frames--;
//		if (frames < 0) {
//			print ("done");
//			frames = 30;
//		}
	}
}
